apiVersion: radix.equinor.com/v1
kind: RadixApplication
metadata:
  name: sondre-react-test
spec:
  environments:
    - name: production
      build:
        from: main
  components:
    - name: frontend
      src: ./
      ports:
       - name: http
         port: 80
      publicPort: http